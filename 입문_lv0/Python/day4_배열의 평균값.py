def solution(numbers):
    answer = 0
    
    answer = sum(numbers)/len(numbers)
    
    return answer

#### 파이썬 평균값 구하기
import numpy || import statistics
avg = numpy.mean(arr)

for val in arr:
    result += val